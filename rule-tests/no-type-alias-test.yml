id: no-type-alias
valid:
  - |
    struct Store;
    impl TryFrom<u8> for Store {
        type Error = String;
        fn try_from(value: u8) -> Result<Self, Self::Error> {
            Ok(Store)
        }
    }
invalid:
  - type StoreError = Box<dyn std::error::Error + Send + Sync>;
  - pub type Staged = (&'static str, String);
  - |
    fn body() {
        type Local = u8;
    }
