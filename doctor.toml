# Read-only checks for `just doctor`.

[[tool]]
name = "cargo"
min = "1.75"
hint = "install rust via rustup: https://rustup.rs"

[[tool]]
name = "just"
hint = "cargo install just"

# Required by `just test --all`.
[[tool]]
name = "ast-grep"
hint = "cargo install ast-grep --locked"
