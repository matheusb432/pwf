[package]
name = "prompt-lanes"
version = "0.1.0"
edition.workspace = true
publish.workspace = true
description = "prompt-lanes: domain-agnostic one-line lane-syntax task-prompt parser with pluggable render adapters"

[dependencies]

[dev-dependencies]
criterion = { workspace = true }

[[bench]]
name = "prompt_lanes"
harness = false

[lints]
workspace = true
