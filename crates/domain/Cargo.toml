[package]
name = "pwf-domain"
version = "4.1.0"
edition = "2024"
publish = false

[dependencies]
nutype = { version = "0.7.0", features = ["serde", "regex"] }
thiserror = "2.0.18"

[lints]
workspace = true
