[package]
name = "pwf-core"
version = "0.1.0"
edition.workspace = true
publish.workspace = true
description = "pwf-core: domain-neutral vault primitives for pwf"

[dependencies]
thiserror = { workspace = true }
regex = { workspace = true }
chrono = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }

[lints]
workspace = true
