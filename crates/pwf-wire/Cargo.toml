[package]
name = "pwf-wire"
version = "0.1.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "pwf_wire"
path = "src/lib.rs"

[dependencies]
pwf-models = { workspace = true }

[lints]
workspace = true
