[package]
name = "pwf-note"
version = "0.1.0"
edition = "2024"
publish = false
description = "pwf-note — one-liner project notes engine over pwf-core"

[dependencies]
pwf-core = { path = "../pwf-core" }
regex = "1"
thiserror = "2.0.18"

[dev-dependencies]
tempfile = "3"
