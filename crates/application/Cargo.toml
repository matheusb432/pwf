[package]
name = "pwf-application"
version = "4.1.0"
edition = "2024"
publish = false

[dependencies]
pwf-domain = { path = "../domain" }
cqrsy = { workspace = true, features = ["derive"] }
thiserror = "2.0.18"

[dev-dependencies]
tempfile = "3"

[lints]
workspace = true
